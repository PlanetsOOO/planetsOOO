# Keep production deploys lean (site only). Extension packages ship separately.
extension/
dist/
scripts/.pydeps/
# Root data/ only — do not use bare "data/" or it also strips src/data/.
/data/
*.zip
.cursor/
