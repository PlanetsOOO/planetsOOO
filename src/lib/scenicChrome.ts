/** Hide scenic menu after this idle period (ms). */
export const SCENIC_CHROME_IDLE_MS = 10_000;

/** Hide flight-mode center reticle after this idle period (ms). */
export const FLIGHT_RETICLE_IDLE_MS = 10_000;
